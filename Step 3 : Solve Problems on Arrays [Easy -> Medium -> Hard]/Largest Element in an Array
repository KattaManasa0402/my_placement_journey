class Solution {
  public:
    int largest(vector<int> &arr) {
        int maxel=INT_MIN;
        for(int i=0;i<arr.size();i++){
            maxel=max(arr[i],maxel);
        }
        return maxel;
    }
};
